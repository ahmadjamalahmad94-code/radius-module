-- ════════════════════════════════════════════════════════════════════════
-- 171 — subscribers.login_without_password: الدخولُ بالاسم وحدَه
--
-- امتدادٌ للهجرة 170 (التي غطّت البطاقات). تبيّن أنّ شبكةَ «سمير» ليست
-- بطاقاتٍ فقط: `radcheck` في لوحته يحمل `Cleartext-Password := ''` لـ**41
-- من 42** حسابًا غير بطاقة — أي أنّ مشتركيه أيضًا يدخلون باسمهم وحدَه.
--
-- 🔴 وبدون هذا العمود كان التحويل يقطعهم: `_check_password` ترفض حتمًا
--    (`if req.chap_password: if not sub.password: reject`) ومايكروتيك
--    هوت-سبوت يستعمل CHAP افتراضًا. مقيسٌ حيًّا على خادمه: المشترك `2050`
--    (صالحٌ حتى 16/9) رُفض بـ«كلمة المرور غير صحيحة».
--
-- 🔑 العلَم **لكلّ مشترك** وافتراضُه 0 ⇒ لا شبكةَ أخرى تتأثّر، والقرارُ
--    لصاحب الشبكة. ورقمُ/اسمُ المشترك هو السرّ — كما كان على لوحته.
-- ════════════════════════════════════════════════════════════════════════

ALTER TABLE subscribers ADD COLUMN login_without_password INTEGER NOT NULL DEFAULT 0;
