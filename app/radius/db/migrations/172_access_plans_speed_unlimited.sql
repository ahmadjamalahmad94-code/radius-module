-- ════════════════════════════════════════════════════════════════════════
-- 172 — access_plans.speed_unlimited: «بلا حدّ» قرارٌ صريحٌ لا صفرٌ صامت
--
-- كان صفرُ السرعة يمرّ في التحقّق (الشرطُ >= 0) ثمّ يخرج الردُّ بلا
-- `Mikrotik-Rate-Limit` فيطبّق الراوترُ ملفَّه الافتراضيَّ — وهو عادةً
-- بلا حدّ. أيْ إنّ «نسيتُ أكتب السرعة» و«أريدها مفتوحة» كانا الشيءَ
-- نفسَه. واقعةُ «شركتي» ‏2026-09-12: باقة «Default service» ‏0/0 عليها
-- ‏210 بطاقاتٍ و‏3 مشتركين مفتوحين بلا أن يعلم أحد.
--
-- الآن: الصفرُ يُرفض عند الحفظ ما لم يُعلَّم هذا العمود. والباقاتُ القائمةُ
-- بصفرٍ تبقى كما هي (لا نخمّن نيّةَ المشغّل) لكنّ المحرّك يُسجّل ERROR
-- بعلامةٍ ثابتة كلَّما خرج ردٌّ بلا سرعةٍ لباقةٍ غيرِ معلَّمة.
-- ════════════════════════════════════════════════════════════════════════
ALTER TABLE access_plans ADD COLUMN speed_unlimited INTEGER NOT NULL DEFAULT 0;
