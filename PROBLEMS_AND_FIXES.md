# المشاكل والحلول

> **سياسةُ التعامل** (قرارُ المالك، 2026-08-27):
> - **لا نلمس خادمًا يعمل بلا شكوى.** الإصلاحُ جاهزٌ هنا ويُطبَّق **عند الشكوى**.
> - **كلُّ نشرٍ جديد** (خادمٌ جديد أو نسخةٌ لم تُستعمل بعد) يأخذ التعديلات كلَّها.
> - كلُّ بندٍ هنا مكتوبٌ بترتيب: **ما يقوله الزبون → كيف نتأكّد → ما ننفّذه**.

---

## ① العددُ في «المتصلون الآن» أقلُّ من الحقيقة

**ما يقوله الزبون:** «في بطاقات بالأكتيف على الراوتر ومش موجودة بالمتصلين».

**السبب:** مهلةُ فتح API المايكروتيك مقصوصةٌ على **٣ ثوانٍ**
(`_DEFAULT_CONNECT_TIMEOUT` في `app/radius/integration/mikrotik/client.py`).
راوترٌ خلف نفقٍ بعيد (تأخير ٨٠–٩٠ م.ث) تكفيه وميضةُ تأخيرٍ لتجاوزها، فيُعلَن
«غير قابل للوصول» — و**بالتصميم** لا تُعرَض جلساتُ راوترٍ لا يمكن التحقّق منه.

**كيف نتأكّد:**
```bash
docker logs --since 10m hoberadius 2>&1 | grep -c unreachable   # > 0 ؟
ping -c 3 <ip_الراوتر>            # يردّ بلا فقد ⇒ الراوتر حيّ والإنذار كاذب
```
مقيسٌ عند عبد: **٥٢٩ سطرَ فشل** بينما `ping` ٠٪ فقد وزمنُ فتح API الحقيقيّ
**0.33–0.70 ثانية**. و**١٣ جلسةً** كانت مخفيّةً وأصحابُها متّصلون.

**العلاج** — في `/opt/hoberadius/.env` (**الجذر، لا `deploy/`** — الكومبوز
يقرأ `env_file: ../.env`):
```
HOBERADIUS_MT_CONNECT_TIMEOUT_SEC=8
```
ثمّ `cd /opt/hoberadius/deploy && docker compose up -d hoberadius`.
النتيجة عند عبد: **صفرُ فشل**.

**ومعه تعديلٌ برمجيّ** (`3af5e3cd`): شريطٌ فوق القائمة يقول أيُّ راوترٍ تعذّرت
قراءتُه وكم جلسةً أُخفيت. كانت شاشةُ «غير متصل» تظهر **فقط حين تفرغ القائمة
كلُّها**، فسقوطُ راوترٍ من ثلاثةٍ يمرّ بلا تفسير.

---

## ② «الوقت المستخدم» أكبرُ من الحقيقة

**ما يقوله الزبون:** «البطاقة عشر ساعات والشاشة تقول استهلك ١٠٫٦ وهو ما دخل
إلّا ٥». وأخطرُ منه: بطاقةٌ **تُحاسَب بالثانية** تموت في نصف وقتها.

**السبب:** `mt_reconciler._materialize_nas` كان يبحث عن صفٍّ **مفتوح** وعلى
**نفس `nasipaddress`** وحدَه قبل أن يكتب صفَّ جلسةٍ صناعيًّا (`mtsync-`).
فتنشأ نسخةٌ ثانيةٌ في حالتين: أُغلق الصفُّ الحقيقيُّ خطأً والجلسةُ حيّة · أو
سجّله فري-رديوس بالعنوان **العامّ** ونحن نستطلع عنوانَ النفق. والنسختان
تُجمعان خامًا في العرض **وفي `policy_engine._accounted_seconds`**.

**كيف نتأكّد** — قارن الجمعَ الخام باتّحاد الفترات **لكلّ (مستخدم، جهاز)**:
> ⚠️ لا تجمع بالاسم وحدَه: **جهازان متوازيان ليسا تكرارًا**، وقياسٌ كهذا يَعُدّ
> الاستعمالَ المشروعَ عطبًا. (أخطأتُ فيه مرّتين قبل تصحيحه.)

مقيسٌ عند عبد: **٢١٦ ساعةً (٣٪)** على ١٨٦ حسابًا · و١٠٩٤ صفَّ `mtsync` من ٨٠٨٠.

**العلاج:**
1. **منعُ التكرار الجديد** — `dcda6299` (`_covered_by_real_row`، ٥ اختبارات).
2. **إصلاحُ القديم** — `/tmp/repair_mtsync.py`: يُقلّم صفوف `mtsync` **المغطّاة
   ≥٩٠٪** بصفٍّ حقيقيّ إلى مساهمتها الحصريّة. **لا يحذف صفًّا ولا يمسّ صفَّ
   فري-رديوس**، ويأخذ نسخةً احتياطيّةً ويكتب سجلَّ تراجعٍ `radacct_mtsync_repair`.
   نُفّذ عند عبد: **٤٨ صفًّا · ٣٦٫٣ ساعة**.

---

## ③ تحويلُ نمط الحساب لا يقع فعليًّا

**ما يقوله الزبون:** «حوّلتُ الحزمة من محاسبة بالثانية إلى من أوّل اتّصال (أو
العكس) وما صارت».

**الحقيقة:** الاتّجاهُ **بالثانية ⟵ من أوّل اتّصال يعمل** (مقيسٌ على «حسن10»:
٢٠٥ بطاقةً خُتمت `أوّل اتّصال + ١٠س`، و٩٦١ جلسةً بلا تجاوزٍ واحد).
والاتّجاهُ **المعاكس لا يعمل** — ثلاثةُ أوجهٍ للفشل:

| # | العطب | الأثر |
|---|---|---|
| ① | المطابقة تُستدعى بتغيير **المدّة** فقط | قلبُ النمط عبر الـAPI ⇒ الحزمةُ بنمطٍ والبطاقاتُ بآخر |
| ② | حسابُ النافذة أعمى عن النمط | «بالثانية» تُختم بساعةِ حائطٍ من `time_value` ⇒ تموت في الدرج |
| ③ | المطابقة تنسحب عند نافذةٍ صفر | يبقى ختمُ النمط السابق قائمًا |

🔑 **في «بالثانية» ‏`time_value` رصيدُ استخدامٍ لا نافذةَ تقويم.** السقفُ
التقويميُّ الصحيح لهذا النمط `validity_after_first_login_days` وحده.

⚠️ وشاشةُ الويب تنجو من ① مصادفةً: نموذجُ التعديل يرسل **كلّ** الحقول. ولهذا
يُسجّل التدقيقُ ٢٧ حقلًا «متغيّرًا» عند قلبِ زرٍّ واحد — `changed_fields` ليس
فرقًا بل قائمةَ المُرسَل.

**العلاج:** `a8bee6ae` (٤ اختبارات، اثنان يسقطان على الشيفرة القديمة).

---

## ④ التحويل يقتل بطاقاتٍ بأثرٍ رجعيّ

**ما يقوله الزبون:** «حفظتُ الحزمة وفجأةً وقفت بطاقات كثيرة».

**السبب:** المطابقةُ تُعيد الحساب من **أوّل دخول** البطاقة. بطاقةٌ كانت
«بالثانية» استهلكت ستّ دقائقَ من عشر ساعاتٍ تصير نافذتُها «أوّل دخولها + ١٠س»
— وقد مضت. مقيسٌ عند عبد يوم ٢٦/٨: **٧١ من ٨٦ ماتت لحظةَ الحفظ · ٣٤٥٫٧ ساعةً
ضاعت** — والمشغّل لم يرَ إلّا «تم الحفظ».

**العلاج:** `e402bb21` — يُعَدّ `expired_now` ويظهر تحذيرٌ صريح:
«⚠️ انتهت فورًا N بطاقةً بدأت سابقًا — راجعها قبل أن يتّصل أصحابُها».

📌 **التعويضُ لا يُنفَّذ تلقائيًّا** (قرارُ المالك) — منحُ وقتٍ لزبونٍ قرارُ
صاحب الشبكة لا قرارُنا.

---

## ⑤ بطاقةٌ تعمل بعد انتهاء مدّتها بيومٍ أو أكثر

**ما يقوله الزبون:** «البطاقة عشر ساعات وخلصت، بس لسا شغّالة بعد يومين».

**السبب:** `policy_engine` يختم `first_used_at = COALESCE(first_used_at, now)`
— **لا يُعيد الختمَ أبدًا**، وهذا صائب. لكن إن مرّ دخولٌ **بلا** محرّك السياسة
(مصادقةُ فري-رديوس محلّيًّا) فالبطاقة تخدم وتُسجَّل في `radacct` و`first_used_at`
يبقى **فارغًا** ⇒ لا `expire_at` ⇒ لا انتهاء. ثمّ أوّلُ دخولٍ يمرّ بالمحرّك
يختمها **من ذلك اليوم** ⇒ نافذةٌ كاملةٌ ثانية.

**كيف نتأكّد** — قارن الختمَ بأقدم محاسبة **لا** بالنافذة:
```sql
cards.first_used_at > (SELECT MIN(acctstarttime) FROM radacct
                        WHERE username = cards.username) + دقائق
```
مقيسٌ على «حسن10»: **٦ بطاقاتٍ من ٢٠٣ · ٧٣ ساعةً ممنوحةً خطأً** (أسوأها ٢٢٫٨س).

📏 **والبحثُ عن «جلسةٍ تجاوزت المدّة» لا يكشف هذا**: الجلساتُ كلُّها داخل
نوافذها، والنافذةُ نفسُها هي المزوَّرة. اسأل: *هل بدايةُ العدّ صادقة؟*

**العلاج المؤقّت:** أعِد الختمَ إلى `MIN(acctstarttime)` ثمّ
`expire_at = هو + المدّة`.
**العلاج الجذريّ (لم يُنفَّذ بعد):** عند الختم خُذ **الأقدمَ** بين «الآن» وأوّل
سجلٍّ في `radacct`، فلا يُكافَأ دخولٌ فاتَ المحرّك بنافذةٍ جديدة.

---

## حالةُ النشر

كلُّ ما سبق على الفرع **`agent/mtsync-duplicate-session-rows`**
(`dcda6299` · `a8bee6ae` · `3af5e3cd` · `e402bb21`).

| الخادم | الحالة |
|---|---|
| عبد أبو هاشم (188.40.63.13) | ✅ الأربعةُ منشورة · `.env` مضبوط · بياناتُه مُصلَحة |
| client1 · client2 · client4 | ⬜ **عند الشكوى فقط** |
| `main` | ⬜ يُدمج عند أوّل نشرٍ جديد |

**عند نشرٍ جديد:** ادمج الفرع، وأضف `HOBERADIUS_MT_CONNECT_TIMEOUT_SEC=8` إلى
`.env` إن كان الراوترُ خلف نفقٍ بعيد.

---

## إخفاقاتُ اختباراتٍ سابقةٌ — ليست منّا ولا تُطارَد
`test_checker_strip_both_states` (٢) · `test_online_list_separation` (٢) —
تفشل على النقطة السابقة لهذه التعديلات أيضًا. مُتحقَّقٌ منها 2026-08-27.
