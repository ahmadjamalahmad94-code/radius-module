Flask>=3.0,<4.0
# أساس التدويل (i18n): Flask-Babel يدمج gettext مع Jinja ويوفّر أداة pybabel
# للاستخراج/التحديث/الترجمة. لغة المصدر عربية، والكتالوجات تترجم للغات الأخرى.
Flask-Babel>=4.0,<5.0
gunicorn>=21.2
openpyxl>=3.1,<4.0
reportlab>=4.2,<5.0
Pillow>=10.0,<12.0
arabic-reshaper>=3.0,<4.0
python-bidi>=0.6,<1.0
# Phase M — X25519 keypair generation for WireGuard peers.
# Standard library doesn't ship Curve25519 primitives; cryptography
# does (and is heavily audited).
cryptography>=42.0,<46.0
# Hotspot card import engine — reads tabular PDFs (the most common
# delivery format). The engine falls back to pypdf and then to a
# clear error if neither is installed, so the dependency is "strongly
# recommended" not strictly required.
pdfplumber>=0.11,<1.0
# QR codes for the Telegram one-click connect deep link (t.me/<bot>?start=…).
# Pure-Python, MIT, zero transitive deps. services/qr_svg.py degrades to a
# placeholder if it is ever missing, so the deep-link button still works.
segno>=1.6,<2.0
