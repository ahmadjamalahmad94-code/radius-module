# Handoff — Session of 2026-05-20

> ملف حفظ الحالة قبل النوم. نرجع له بكرا الصبح ونكمل من النقطة الأخيرة بدون
> ما نفقد سياق.

---

## 1. آخر شي عمله (أعلى الكومة)

**آخر commit:** `7687f92` — *MT script — survive non-bound DHCP leases
(status=bound + on-error)*

السكربت الجديد على صفحة `/admin/radius/mt-push-setup` يولّد نسخة محصّنة من
الـ MT scheduler:
- يفلتر بـ `status=bound` فقط
- يلف كل `/ip/dhcp-server/lease/get` بـ `:do { } on-error={}`
- النتيجة: scheduler ما يفشل لو فيه lease ناقص بيانات

**رفعت كل شي على origin/main.** الفرع نظيف.

---

## 2. الحالة الحالية للـ DHCP push pipeline

### ✅ ما اشتغل (مؤكَّد عمليًا)

| المكوّن | الحالة | دليل |
|---|---|---|
| Ingest endpoint `/api/v1/devices/ingest` | 200 OK | `curl` test أرسل `vps-test` و `mt-manual-test` ونجح |
| MT → VPS connectivity (HTTP) | يعمل | Test 1 من الراوتر: GET `/api/v1/devices` رجع `count=1` |
| Token auth | يعمل | `hr_NeFx_j52a_dnZ0D-NIfnCXBqXAqyIRRkv97ZsO708jw` صالح |
| JSON parser (class_id → os) | يعمل | `android-dhcp-11 → os=android, version=11` |
| DB upsert | يعمل | الـ row `aa:bb:cc:dd:ee:ff` موجود في `device_fingerprints` |
| Auto-refresh + pagination في جدول الجلسات | يعمل | تم نشره |
| Per-session disconnect picker | يعمل | بعد إصلاح session-id mismatch + scoped-close |
| Lock-MAC enforcement (يطرد غير المطابقين) | يعمل | flash message يبيّن العدد |
| Freeze يقفل الجلسات النشطة | يعمل | `disable_card` يستدعي `disconnect` |
| MAC manage (edit vs full-unlock) | يعمل | صندوق فيه 3 خيارات |
| MT reconciler — يقفل ghost sessions | شغّال (logs تثبت) | كل 30s |
| Diagnostics page + unreachable banner | يعمل | الـ heartbeat يغذي البانر |

### ⏳ ما لم نتحقّق منه بعد (التابع للجلسة القادمة)

| البند | المطلوب لإثباته |
|---|---|
| الـ scheduler الجديد (commit 7687f92) يدفع leases فعلًا | إعادة paste السكربت ثم `curl /api/v1/devices` يطلع >2 صفوف |
| عمود «الجهاز/النظام» يمتلئ في Card Checker | افتح أي بطاقة ولاحظ |
| الـ MT ↔ Radius API direct على بورت 8728 | لسا مسكّر — pull mode معطّل عمليًا، نعتمد فقط على push |

---

## 3. أوامر الـ resume السريعة (لما نرجع بكرا)

### على الـ VPS

```bash
cd /opt/hoberadius
git pull
docker compose -f deploy/docker-compose.yml build hoberadius
docker compose -f deploy/docker-compose.yml up -d hoberadius
docker compose -f deploy/docker-compose.yml logs --tail=60 hoberadius | grep -E "mt_reconciler|device_fingerprint|workers"
```

### على المايكروتيك

```routeros
# 1) احذف القديم
/system scheduler remove [find name=hoberadius-push-dhcp]

# 2) ارجع للصفحة، انسخ السكربت الجديد (نسخة 7687f92 فيها status=bound)
#    http://187.77.70.18/admin/radius/mt-push-setup
#    والصقه هنا.

# 3) شغّله فورًا (الـ run command المضمون لكل إصدارات RouterOS)
:execute [/system scheduler get [find name=hoberadius-push-dhcp] on-event]

# 4) شوف اللوج
/log print where message~"fetch" without-paging
```

### على الـ VPS — اختبار النجاح

```bash
TOK="hr_NeFx_j52a_dnZ0D-NIfnCXBqXAqyIRRkv97ZsO708jw"
curl -s -H "Authorization: Bearer $TOK" \
     "http://187.77.70.18/api/v1/devices?limit=50" \
     | python3 -m json.tool | head -40
```

**علامة النجاح:** أكثر من 2 صفوف (يعني أكثر من vps-test و mt-manual-test).

---

## 4. القطع المعمارية الي بنيناها هاليوم

> كله موجود في الكود + موثّق في commit messages. هذا فقط جدول مرجعي.

### Backend جديد

| ملف | الوظيفة |
|---|---|
| `app/radius/db/migrations/026_device_fingerprints.sql` | جدول DHCP fingerprints |
| `app/radius/db/repos/device_fingerprints_repo.py` | upsert/lookup |
| `app/radius/services/device_fingerprint_sync.py` | parse class-id + hostname، Pull mode |
| `app/workers/device_fingerprint_worker.py` | worker كل 120s (Pull) |
| `app/workers/mt_reconciler.py` | worker كل 30s — يقفل ghost sessions |
| `app/api/v1/devices.py` | `/by-mac`, `/list`, `/sync`, `/ingest` |
| `app/radius/services/mt_diagnostics.py` | TCP + API probes للراوترات |

### Frontend جديد

| صفحة | الرابط |
|---|---|
| تشخيص الراوترات | `/admin/radius/diagnostics` |
| دفع DHCP من المايكروتيك | `/admin/radius/mt-push-setup` |
| بانر "MT غير متاح" | في كل صفحة (context_processor) |
| pagination + auto-refresh في الجلسات | داخل Card Checker |
| Manage MAC lock (edit/unlock) | داخل Card Checker |

---

## 5. الـ issues المعلّقة (لو الـ scheduler الجديد لسا ما اشتغل)

في حال جربت السكربت الجديد بكرا ولسا ما وصلت بيانات، احتمالات تشخيص:

1. **`/log print` يطلع أخطاء fetch** → نفحص أكثر، احتمال JSON malformed
2. **`/log print` فاضي** → الـ scheduler ما يفير. تحقق:
   ```routeros
   /system scheduler print detail where name=hoberadius-push-dhcp
   ```
   شوف `run-count`. لو = 0، الـ scheduler ما اتفعّل.
3. **بيانات وصلت لكن ما تظهر في Card Checker** → cache في الـ DHCP enrichment، أو الـ MAC normalization مختلف. SQL مباشر:
   ```bash
   docker compose -f deploy/docker-compose.yml exec hoberadius \
       python3 -c "import sqlite3; c=sqlite3.connect('/app/instance/hoberadius.db'); \
       print([dict(r) for r in c.execute('SELECT mac, hostname FROM device_fingerprints')])"
   ```
4. **عدد الـ leases الفعلي على الراوتر**:
   ```routeros
   :put [:len [/ip/dhcp-server/lease/find status=bound]]
   ```

---

## 6. ملاحظة مهمة عن الـ MT API الـ pull mode

- البورت `8728` على الراوتر `213.6.169.138` مسكّر من جهة الـ ISP/firewall
- الـ workers يحاولون كل 30s/2m ويفشلون (timeout)
- البانر البرتقالي يظهر بسبب هذا — **هذا متوقّع** حتى نفعّل API على الراوتر أو نستخدم الـ push mode حصرًا
- الـ push mode (السكربت الجديد) **لا يحتاج فتح أي بورت** — يستخدم outbound HTTPS فقط

**القرار:** نعتمد على push mode حتى نحلّ مشكلة الـ inbound API. لا داعي للقلق من البانر البرتقالي حاليًا — هو تذكير، مش خلل.

---

## 7. أرقام مهمة للرجوع لها

- **آخر commit:** `7687f92`
- **Token (للاختبار):** `hr_NeFx_j52a_dnZ0D-NIfnCXBqXAqyIRRkv97ZsO708jw`
- **VPS:** `srv1681984` على `187.77.70.18`
- **MT router:** `213.6.169.138`
- **NAS داخلي:** `10.10.0.2`
- **بطاقة الاختبار:** `7772`
- **Tenant id:** `1`

---

## 8. ما الـ user قد يطلبه بكرا

ذكر "هعطيك شغل تعمله للصبح" — يعني عنده مهمة جاهزة. توقّع:
- إما إكمال المهمة الحالية (تجربة السكربت الجديد + إثبات أن عمود الجهاز ممتلئ)
- أو مهمة جديدة بالكامل (نطلع توجيهات منه)

نبدأ بكرا بـ:
1. سؤاله: "تبدأ بإكمال DHCP push أم عندك مهمة جديدة؟"
2. لو جديدة → استمع له ونفّذ
3. لو يكمل → نفّذ خطوات الـ resume أعلاه

---

**انتهى الحفظ.** نوم هنيء 🌙
