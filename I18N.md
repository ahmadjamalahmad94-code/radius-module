# دليل التدويل (i18n) — وحدة RADIUS / Hobe Hub

مرجع عملي قائم بذاته لتعميم الترجمة على المشروع. يتبعه كل وكيل/منفّذ
لتغليف النصوص وترجمتها دون كسر الواجهة العربية. كل المسارات وأسماء الدوال
الواردة هنا حقيقية ومطابقة للكود.

---

## 1) القرار المعماري ولماذا

**الحل: Flask-Babel 4.0** (مثبّت بالفعل) — الغلاف القياسي لـ gettext في
Flask. يدمج الترجمة مع Jinja تلقائيًا (`{{ _('...') }}`، `{% trans %}`)،
ويوفّر أداة `pybabel` لاستخراج/تحديث/تصريف الكتالوجات. خفيف ومبني فوق
مكتبة Babel الناضجة.

**النموذج المعتمد: «لغة المصدر = العربية».**
النص العربي نفسه هو الـ `msgid`، وكتالوج الإنجليزية (`translations/en`)
يترجمه. الأثر العملي:

- التعميم **ميكانيكي**: تغلّف النص العربي بـ `_( )` فقط، ثم `pybabel`
  يلتقطه، ثم تُملأ ترجمته الإنجليزية لاحقًا.
- **العربية لا تحتاج كتالوجًا**: عند اختيار العربية يُرجع gettext الـ
  `msgid` نفسه (= النص العربي). لذا الموقع يبقى عربيًا 100% افتراضيًا،
  والترجمة طبقة فوقية اختيارية.
- **لا كسر أبدًا**: أي نص لم يُترجم في `en` يسقط تلقائيًا للعربية (سلوك
  gettext الطبيعي). انظر «قاعدة عدم الكسر» في الأسفل.

**الوحدة الأساسية:** `app/radius/i18n.py` — تُستدعى من
`app/__init__.py::_install_i18n`. تحوي:

| العنصر | الوصف |
|---|---|
| `SUPPORTED_LOCALES = ("ar","en")` | اللغات المدعومة |
| `DEFAULT_LOCALE = "ar"` | لغة المصدر/السقوط النهائي |
| `RTL_LOCALES` | لغات الكتابة من اليمين (`ar, he, fa, ur`) |
| `LANGUAGES` | dict عرض اللغات (الاسم/العلم/الاتجاه) لقائمة المبدّل |
| `select_locale()` | المنتقي (سلّم الأولوية أدناه) |
| `current_locale()` | رمز اللغة الفعّالة كنص (`'ar'`/`'en'`) |
| `text_dir()` | اتجاه الكتابة (`'rtl'`/`'ltr'`) |
| `init_i18n(app)` | التهيئة + حقن متغيّرات القوالب |

**سلّم أولوية اختيار اللغة** (`select_locale()`):

1. `session['locale']` — اختيار المستخدم الصريح من قائمة الهيدر.
2. `session['admin_locale']` — لغة المسؤول المسجَّل (عمود `admins.locale`،
   migration 104).
3. `general.default_locale` — الإعداد العام عبر
   `tenants_repo.get_setting(tid, 'general.default_locale')`.
4. `'ar'` — السقوط النهائي الآمن.

(المنتقي آمن تمامًا: أي خطأ في القراءة يسقط للعربية بدل كسر الصفحة.)

**مبدّل اللغة:** المسار `radius.set_locale` في
`app/radius/routes/i18n_routes.py` على
`/admin/radius/set-locale?locale=en&next=...` — يحفظ الاختيار بالجلسة،
ويثبّته في `admins.locale` للمسؤول، ثم يعيد التوجيه إلى `next`.

**متغيّرات القوالب المحقونة** (متاحة في كل قالب عبر context_processor):
`get_locale()`, `text_dir`, `LANGUAGES`, `SUPPORTED_LOCALES`,
`current_locale`.

**القالب الجذري** يستخدم:
```jinja
<html lang="{{ get_locale() }}" dir="{{ text_dir }}">
```
**الخط:** المتغيّر `--hub-font` يتبدّل تلقائيًا — Cairo/Almarai للعربية،
ويصبح Inter عند `html[dir="ltr"]` (الإنجليزية).

---

## 2) كيف تغلّف نصًا في قالب (Jinja)

**النص البسيط:** لُفّ النص العربي بـ `_( )`:
```jinja
<h2>{{ _('تسجيل الدخول') }}</h2>
<button>{{ _('إضافة مشترك') }}</button>
```

**سمات HTML** (placeholder, title, aria-label, alt …): تُغلّف أيضًا — هي
نصوص عرض:
```jinja
<input placeholder="{{ _('بحث بالاسم أو اسم الدخول...') }}">
<button aria-label="{{ _('إظهار كلمة المرور') }}">
```

**المتغيّرات داخل النص:** استخدم تنسيق gettext `%(name)s` (وليس تضمين
Jinja داخل النص)، فهذا يُبقي رسالة ترجمة واحدة مستقرّة:
```jinja
{{ _('مرحبًا %(user)s، لديك %(count)s إشعارًا', user=name, count=n) }}
```
> لا تكتب `{{ _('مرحبًا ') }}{{ name }}` — هذا يجزّئ الرسالة ويعطّل ترتيب
> الكلمات بين اللغات.

**الجمع (plural):** استخدم `{% trans %}`:
```jinja
{% trans count=n %}مشترك واحد{% pluralize %}{{ count }} مشتركين{% endtrans %}
```

**النصوص متعددة الأسطر / الفقرات الطويلة:** `{% trans %}...{% endtrans %}`
أوضح من `_( )` للنص الطويل:
```jinja
{% trans %}
  إدارة كاملة لخدمات المصادقة والمشتركين والكروت والشبكة من مكان واحد.
{% endtrans %}
```

**نصوص JavaScript داخل القالب (بثّ Jinja):** لا تترك نصًا عربيًا خامًا
داخل `<script>`. مرّره من Jinja مغلّفًا بـ `_( )` (عبر `tojson` لأمان
الاقتباس):
```jinja
<script>
  const MSG = {
    confirmDelete: {{ _('هل تريد الحذف فعلًا؟')|tojson }},
    saved:         {{ _('تم الحفظ')|tojson }},
  };
</script>
```
> أداة القياس تُصنّف العربية داخل `<script>`/`<style>` منفصلة بعلم
> `[JS/CSS]` لأنها تحتاج هذه المعالجة الخاصة.

---

## 3) كيف تغلّف في بايثون

```python
from flask_babel import gettext as _, lazy_gettext as _l
```

**`gettext` (`_`)** — للنصوص التي تُقيَّم **داخل سياق طلب** (داخل دالة view،
عند بناء رسالة flash، استجابة JSON…):
```python
flash(_('تم حفظ التغييرات'), 'success')
return jsonify(error=_('غير مصرّح')), 403
```

**`lazy_gettext` (`_l`)** — للنصوص المُعرَّفة **خارج سياق الطلب** ووقت
الاستيراد: ثوابت الوحدة، عناوين الأعمدة، خيارات القوائم، رسائل النماذج،
تسميات `enum`. تُقيَّم كسولًا عند العرض فلا تُجمَّد على لغة الاستيراد:
```python
STATUS_LABELS = {
    'enabled':  _l('مفعَّل'),
    'disabled': _l('معطَّل'),
    'expired':  _l('منتهٍ'),
}
```
> القاعدة: إن كان النص يُنشأ مرّة واحدة على مستوى الوحدة → `_l`. إن كان
> يُنشأ داخل دالة تعمل لكل طلب → `_`.

(إعداد الاستخراج `babel.cfg` يضمّ `-k _l` ليلتقط `lazy_gettext` أيضًا —
انظر الأوامر أدناه.)

---

## 4) ماذا لا تغلّف

**لا تلفّ** أيًّا مما يلي — تغليفه يلوّث الكتالوج أو يكسر المنطق:

- أكواد/معرّفات: أسماء الحقول، `name="username"`, مفاتيح القواميس،
  قيم `enum` البرمجية (`'enabled'` مفتاحًا، لا قيمته المعروضة).
- `url_for(...)`, المسارات، الروابط، `href`, `action`.
- الأرقام، التواريخ الخام، المعرّفات، رموز العملات كرموز.
- كلاسات CSS، الأنماط، أسماء أيقونات Font Awesome (`fa-user`).
- البيانات الديناميكية القادمة من قاعدة البيانات (أسماء المشتركين،
  قيم السجلات) — تُعرض كما هي.
- النصوص اللاتينية الخالصة (لا تحوي عربية) — خارج نطاق هذا النموذج.

> القاعدة العملية: غلّف **ما يقرؤه الإنسان كنصّ عرض عربي**، واترك **ما
> يقرؤه الحاسوب**.

---

## 5) سير عمل الترجمة الكامل (بالأوامر)

> **لا تشغّل `extract`/`compile` عشوائيًا أثناء عمل وكلاء آخرين** على
> الكتالوج لتفادي التعارض — هذا القسم توثيقي. شغّلها عند تجميع دفعتك.

البنية: `babel.cfg` بجذر المشروع (مهيّأ لـ python + jinja2)، والكتالوجات
في `translations/<locale>/LC_MESSAGES/messages.po` (+ `.mo` بعد التصريف).
موجود `ar` و`en`.

**(1) الاستخراج** — يجمع كل `_( )`/`_l( )`/`trans` في قالب POT:
```bash
# الأنماط في babel.cfg (app/**.py …) نسبية لدليل المسح، فامسح من جذر المشروع (.)
pybabel extract -F babel.cfg -k _l -o translations/messages.pot .
```
> أو ببساطة شغّل `python tools/i18n_assemble.py` الذي يستخرج + يملأ `en` من
> شظايا `tools/i18n_fragments/*.po` + يصرّف الكتالوجين دفعة واحدة.

**(2) التحديث** — يدمج الجديد في كتالوجات اللغات مع إبقاء الترجمات
الموجودة:
```bash
pybabel update -i translations/messages.pot -d translations
```

**(3) الترجمة** — حرّر يدويًا `translations/en/LC_MESSAGES/messages.po`
واملأ `msgstr` لكل `msgid` عربي:
```po
msgid "تسجيل الدخول"
msgstr "Sign in"
```
> العربية (`translations/ar`) **لا تحتاج ملء** `msgstr` — اتركها فارغة،
> فيُرجع gettext الـ `msgid` العربي تلقائيًا.

**(4) التصريف** — يبني ملفات `.mo` التي يقرؤها التطبيق:
```bash
pybabel compile -d translations
```

**شظايا الترجمة (مرحلية):** أثناء العمل المتوازي قد تُجمَّع إدخالات بلا
ترويسة في `tools/i18n_fragments/*.po` ثم تُدمج في
`translations/en/.../messages.po`. هذه آلية تنسيق مؤقّتة؛ الحقيقة النهائية
تبقى كتالوج `en` المصرَّف.

---

## 6) كيف تضيف لغة جديدة

مثال: إضافة الفرنسية (`fr`، LTR):

**(1)** أنشئ كتالوجها من القالب POT:
```bash
pybabel init -i translations/messages.pot -d translations -l fr
```

**(2)** في `app/radius/i18n.py` أضف الرمز في موضعين فقط:
```python
SUPPORTED_LOCALES = ("ar", "en", "fr")
LANGUAGES = {
    ...,
    "fr": {"name": "Français", "flag": "🇫🇷", "dir": "ltr"},
}
```
وإن كانت اللغة **RTL** أضفها أيضًا إلى `RTL_LOCALES`.

**(3)** املأ `translations/fr/LC_MESSAGES/messages.po` ثم
`pybabel compile -d translations`.

> لا شيء آخر في الكود يحتاج تعديلًا — المنتقي والاتجاه والخط يتكيّفون
> تلقائيًا من هذه الثوابت.

---

## 7) أداة القياس: `tools/i18n_extract.py`

سكربت بايثون مستقل (مكتبة قياسية فقط) يمسح `app/templates/**/*.html`
ويكشف **النصوص العربية الصلبة غير المغلّفة**، ويقيس نسبة التغطية.

**التشغيل والخيارات:**
```bash
python tools/i18n_extract.py                 # تقرير نصّي عربي كامل + أعلى 10
python tools/i18n_extract.py --summary       # الملخّص الإجمالي فقط
python tools/i18n_extract.py --top 20        # أكثر 20 ملفًا تبقّيًا
python tools/i18n_extract.py --file <path>   # ملف واحد بالتفصيل
python tools/i18n_extract.py --json          # مخرَج JSON للأدوات/التتبّع
```

**ماذا يفعل بدقّة:**
- يتجاهل المُغلَّف (`{{ _('...') }}`، `{{ gettext(...) }}`،
  `{% trans %}...{% endtrans %}`) — أي «تمّ».
- يتجاهل تعليقات Jinja `{# ... #}` وتعليقات HTML `<!-- -->`.
- يُصنّف العربية داخل `<script>`/`<style>` **منفصلة** بعلم `[JS/CSS]`
  لأنها تحتاج بثّ Jinja (انظر القسم 2).
- يكشف نصوص العرض في المحتوى وفي سمات HTML (placeholder/title/aria-label).
- آمن ضد أخطاء الترميز ولا يكسر على أي ملف.
- **التغطية التقريبية** = المغلّف ÷ (المغلّف + المتبقّي). مؤشّر نسبي
  متّسق، لا عدّ لغوي صارم.

استخدم `--json` لتتبّع التقدّم آليًا بين الدفعات، و`--file` للتحقّق من
ملفٍ بعد تغليفه.

---

## 8) خطة التعميم قطاعًا قطاعًا

إطار ترتيب مقترَح حسب أهمية القطاع وكثافة استخدامه (يملؤه المنفّذون،
ويُقاس التقدّم بعد كل قطاع عبر `--summary`):

1. **الإطار العام**: `_admin_layout.html`, `_sidebar.html`, الهيدر،
   التنقّل، القوالب المشتركة (`_components/`, `_partials/`) — أعلى أثر لأنها
   تظهر في كل صفحة.
2. **المصادقة والدخول**: `login.html`, مبدّل اللغة، رسائل الأخطاء.
3. **المشتركون**: `users_list.html`, `users_form`, `subscribers_overview`,
   نظرة 360.
4. **الكروت**: الإنشاء/الدُفعات/الفاحص/الشحن (`cards_*`).
5. **الباقات**: `plans_form.html`, `plans_list`, مجموعات السرعة.
6. **المحاسبة والمالية**: `finance_center`, التقارير المحاسبية، الفوترة،
   التحصيل.
7. **الشبكة والأجهزة**: الراوترات، السياسات، المسح، `mt_*` (لوحات
   ميكروتك).
8. **الإدارة والصلاحيات**: المسؤولون، مصفوفة الصلاحيات، الموزّعون.
9. **التقارير ولوحات المعلومات**: `dashboard.html`, التقارير، النظرات
   العامة.
10. **الاتصالات والإشعارات**: البوت، الحملات، القوالب، الواتساب.
11. **التوثيق داخل التطبيق** (`docs_*`) والإعداد (`setup_wizard_*`) —
    حجمها كبير لكن أولويتها أدنى.

داخل كل قطاع: غلّف القوالب → شغّل `--file` للتحقّق → بعد القطاع شغّل
`extract`/`update` → املأ ترجمة `en` → `compile`.

---

## 9) قاعدة عدم الكسر

- **العربية تبقى 100% افتراضيًا.** التغليف لا يغيّر ما يراه المستخدم
  العربي إطلاقًا (gettext يُرجع الـ `msgid` العربي).
- **النص غير المترجم في `en` يسقط تلقائيًا للعربية** — لا صفحات مكسورة ولا
  مفاتيح ظاهرة، فقط ظهور النص العربي مؤقّتًا حتى تُملأ ترجمته.
- **المنتقي آمن**: أي خطأ (جلسة/قاعدة بيانات) يسقط للعربية بدل رمي استثناء.
- لذا التعميم **تدريجي وآمن**: غلّف ما تستطيع، اترك الباقي، لا شيء ينكسر.
